#include <iostream>
using namespace std;

int main() {

    int hamburg;
    int beverage;
    int min;
    int sum = 0;

    cin >> hamburg;
    min = hamburg;

    cin >> hamburg;
    if (min > hamburg)
        min = hamburg;

    cin >> hamburg;
    if(min > hamburg)
        min = hamburg;

    sum = min;

    cin >> beverage;
    min = beverage;

    cin >> beverage;
    if(min > beverage)
        min = beverage;

    sum += min;

    sum -= 50;

    cout <<sum<<endl;

}
